
&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если ИмяСобытия = "РС_ФотоСотрудников_ОбновитьФотографию" Тогда
		 Элементы.Фото.Обновить();
	КонецЕсли;
	
КонецПроцедуры
